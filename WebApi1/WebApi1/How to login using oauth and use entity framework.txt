How to login as an user from google or facebook and use entity framework

https://docs.microsoft.com/en-us/azure/app-service-web/web-sites-dotnet-deploy-aspnet-mvc-app-membership-oauth-sql-database

In code first the database is created as a SQL Server LocalDB database

The ASP.NET MVC internet template uses OAuth to expose Facebook, Twitter and Google as Microsoft authentication providers.

canEdit Role to the membership database

using Microsoft.AspNet.Identity;
using Microsoft.AspNet.Identity.EntityFramework


bool AddUserAndRole(ContactManager.Models.ApplicationDbContext context)
 {
     IdentityResult ir;
     var rm = new RoleManager<IdentityRole>
         (new RoleStore<IdentityRole>(context));
     ir = rm.Create(new IdentityRole("canEdit"));
     var um = new UserManager<ApplicationUser>(
         new UserStore<ApplicationUser>(context));
     var user = new ApplicationUser()
     {
         UserName = "user1@contoso.com",
     };
     ir = um.Create(user, "P_assw0rd1");
     if (ir.Succeeded == false)
         return ir.Succeeded;
     ir = um.AddToRole(user.Id, "canEdit");
     return ir.Succeeded;
 }


How to Code First to an existing Database

https://msdn.microsoft.com/en-us/library/jj200620(v=vs.113).aspx